import React from 'react';
import {Box} from 'native-base';

export const MainVideoPlayer = ({
  link,
  onBackPress,
  isLandScape,
}: {
  link: string;
  onBackPress?: Function;
  isLandScape?: boolean;
}) => {
  return <Box></Box>;
};
